# 接续说明

本文件用于记录本项目后续修改的工作约定、任务流程、已完成事项和可避免的问题。以后每次开始新任务前，应先阅读本文件，再读取相关项目文件。

## 项目概况

- 项目路径：`D:\other\cdim.github.io`
- GitHub 仓库：`https://github.com/z1833120/cdim.github.io.git`
- 网站类型：GitHub Pages 静态站
- 主要文件：
  - `index.html`：页面内容、结构、按钮、表格、章节
  - `styles.css`：页面样式、布局、响应式规则
  - `assets/`：论文 PDF、论文页面预览图、实验图表等静态资源

## 每次任务前必须执行

1. 先阅读本文件，确认当前项目约定和历史注意事项。
2. 检查工作区状态：
   - `git status --short`
   - 如有未提交修改，先判断是否与本次任务相关。
3. 读取与任务直接相关的文件，优先少量、精准读取：
   - 查内容用 `rg`
   - 看页面结构优先读 `index.html`
   - 看样式问题优先读 `styles.css`
4. 先把用户需求整理成清晰的任务流，判断先后顺序。
5. 如果需求、素材、发布方式、环境工具存在不确定，不要擅自默认执行；先向用户询问并商议策略。

## 任务规划原则

- 优先处理直接影响用户目标的文件和区域。
- 尽量做局部、可回滚的小改动，不牵扯无关结构、样式和资源。
- 不做用户没有要求的重构、重新设计、删除资源或批量格式化。
- 每次改动前先确认影响范围，改动后立即检查是否有残留引用、错链、布局风险或语义不一致。
- 对公开网站尤其注意：
  - 不暴露未开源论文下载入口。
  - 不泄露账号密码、token、未公开材料。
  - 不发布未经用户确认的大范围内容改写。

## 论文与资源处理规则

- 论文文件不要覆盖原文件。
- 每次论文相关大改动都生成新版本文件，建议使用清晰版本名，例如：
  - `cdim-paper-v2.pdf`
  - `cdim-paper-20260915.pdf`
- 如果只是调整网页是否链接论文，不删除原 PDF，除非用户明确要求删除资源。
- 新增或替换图片/PDF 前，应确认文件名、显示位置和是否需要同步更新页面引用。

## 修改后的检查流程

完成自认为所有任务后，必须从第三方视角重新检查：

1. 功能检查：页面链接、锚点、按钮、图片、PDF 引用是否符合目标。
2. 内容检查：标题、摘要、实验数字、引用信息是否有明显矛盾或遗漏。
3. 样式检查：桌面与移动端是否可能出现文本溢出、重叠、按钮拥挤、图片比例异常。
4. 安全检查：是否意外公开了未开源论文、敏感凭据、未确认材料。
5. Git 检查：
   - `git diff`
   - `git status --short`
6. 若发现新问题，先修复再复查；反复优化到当前任务范围内最稳妥的版本。

## 每轮结束必须更新本文档

- 完成本轮所有任务后，自动更新 `接续说明.md`。
- 更新方式为覆盖原文件，不额外生成 `接续说明-v2.md`、备份版或临时版。
- 更新内容至少包括：
  - 本轮完成了什么。
  - 是否提交、是否推送、对应 commit。
  - 检查中发现并修复了什么。
  - 本来可以避免的坑、原因和下次处理方法。
- 如果本轮没有实际修改网站，也要记录关键决策或待确认事项，避免下次重复沟通。

## 发布流程

- 用户要求同步 GitHub 或确认需要上线时，再执行提交和推送。
- 提交前确认 diff 只包含本轮任务相关内容。
- 推荐流程：
  - `git add <相关文件>`
  - `git commit -m "<简洁英文提交信息>"`
  - `git push origin main`
- 推送成功后提醒用户：GitHub Pages 可能需要几十秒到几分钟刷新。

## 已完成任务记录

### 2026-09-15：移除 Methodology 中的对比图

用户指出页面中展示 decomposition、semantic wrapper、iterative search 对比的图不太好看，要求从网站移除并同步。

已完成：

- 移除 `index.html` 的 Methodology 区域中引用 `assets/figure-comparison.png` 的整个 `figure` 图块。
- 保留同一区域的 `assets/figure-framework.png` 框架图。
- 未删除 `assets/figure-comparison.png` 文件本身，避免影响以后备用或恢复。
- 已全局搜索确认页面中不再引用 `figure-comparison.png`。
- 已提交并准备推送到 GitHub：
  - commit message：`Remove comparison figure`
  - 最终 commit hash 以 `git log --oneline -1` 为准。

### 2026-09-15：移除论文原文下载入口

用户说明论文还没有开源，要求去掉下载论文原文的链接。

已完成：

- 移除首页顶部 `Paper` 按钮。
- 将首页顶部主按钮改为 `Highlights`。
- 移除 `Paper Preview` 区域的 `Open Full Paper` 按钮。
- 保留论文页面预览图和 `BibTeX` 按钮。
- 确认 `index.html` 中没有残留 `assets/cdim-paper.pdf` 链接。
- 已提交并推送到 GitHub：
  - commit：`2b4cc09 Remove paper download links`

## 可避免的问题与处理经验

- 用户曾误发 GitHub 密码。后续不要使用、保存或复述任何账号密码；应提醒用户修改密码，并建议使用本地 git 登录、SSH key 或临时 fine-grained token。
- 删除“下载论文”入口时，不要删除 `assets/cdim-paper.pdf` 文件本身。网页取消公开链接即可，避免误删后续可能还要使用的论文文件。
- 改按钮时要检查同一链接是否在页面多个位置出现。上次 `cdim-paper.pdf` 同时出现在顶部按钮和 `Paper Preview` 按钮中，必须全局搜索确认。
- 若用户问“网站是否已经修改”，要区分本地修改、已提交、已推送、GitHub Pages 已刷新这四个状态，不能混在一起回答。
- 用户要求“把这个图去掉”时，优先删除页面引用而不是删除图片文件；这样既能让网站不显示，又保留后续恢复空间。

## 后续待确认事项

- 如果继续调整页面内容，需要用户明确具体改动目标，例如作者信息、论文状态、实验数据、配色布局、图表替换或中英文版本。
- 如果要重新开放论文下载，需要用户确认论文已开源，并指定最终 PDF 文件名或版本。
